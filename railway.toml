# =================================================================
# 📁 Ruta: /erp13-enterprise/railway.toml  
# 📄 Nombre: railway.toml
# 🏗️ Propósito: Configuración Railway CORREGIDA sin buildCommand problemático
# ⚡ Performance: Usa defaults Nixpacks + deploy optimizado
# 🔒 Seguridad: Sin comandos que rompan Dockerfile
# =================================================================

[build]
# ✅ SIN BUILDCOMMAND PERSONALIZADO - USA NIXPACKS DEFAULTS
# Nixpacks detecta Python automáticamente y hace:
# - pip install -r requirements.txt 
# - Configuración virtual environment
# - Build optimizado automático

[deploy]
# 🚀 COMANDO START ESPECÍFICO PARA TU MAIN.PY
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --worker-class sync --timeout 120 --keep-alive 5 --max-requests 1000 --max-requests-jitter 50 --preload --worker-tmp-dir /dev/shm --log-level info --access-logfile - --error-logfile - --capture-output --enable-stdio-inheritance --pythonpath . wsgi:application"

# 🏥 HEALTH CHECK CONFIGURACIÓN
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# ⏱️ TIMEOUTS OPTIMIZADOS
deployTimeout = 300
startTimeout = 60
stopTimeout = 30

# 🔧 VARIABLES DE ENTORNO CRÍTICAS
[env]
FLASK_APP = "main.py"
FLASK_ENV = "production"
FLASK_DEBUG = "False"
PORT = "8080"
WORKERS = "2"
TIMEOUT = "120"
PYTHONPATH = "."
PYTHONUNBUFFERED = "1"
PYTHONDONTWRITEBYTECODE = "1"
